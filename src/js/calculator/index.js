import schema from "./schema";
import validate from "./validate";
import { calculate, calculatePayback } from "./calculate";

export default {
  schema,
  validate,
  calculate,
  calculatePayback,
};
